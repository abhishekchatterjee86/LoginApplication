# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'LoginApplication' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for LoginApplication

# Architecture
  pod 'RIBs', '~> 0.9'
  
  # FRP
  pod 'RxSwift'
  pod 'RxCocoa', '~> 4.0'

  target 'LoginApplicationTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'LoginApplicationUITests' do
    # Pods for testing
  end

end
